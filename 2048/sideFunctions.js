(function($) {

/*     $.fn.isCellEmpty = function (line, col) {
        if ($.trim($('#' + line + SEP + col).html()) == '') {
            return true;
        }
        else {
            return false;
        }
    }
 */
}(jQuery));